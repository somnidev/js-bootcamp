let temp = 31
let isFreezing = temp <= 32;
console.log(isFreezing)

if (isFreezing) {
    console.log('It is freezing outside!')
}

if (temp >= 110) {
    console.log('It is way too hot outside!')
}

// Challenge area

// Create age set to your age 
// Calculate if is child - if your age is 7 or under - then print message
// Calculate if is senior - if your age is 65 or over - then print message
let age = 65
if (age <=7) {
    console.log('You are going to get a child discount!')
}
if (age >= 65) {
    console.log('You are going to get the senior discount!')
}
